# Marker-Based Test Execution Job Template with Intelligent Agent Routing
#
# Purpose: Routes tests to compatible agents based on pytest markers (@pytest.mark.platform and @pytest.mark.connected_device)
#
# Execution Flow:
# 1. Discovery Job (ASQE-QAMA-VM pool):
#    - Parses test files to extract pytest markers using AST
#    - Queries Azure DevOps API for agent capabilities in ASQE-QAMA-General pool
#    - Generates execution matrix matching test requirements to available agents
#    - Handles device-only markers by expanding to all platforms with that device
#
# 2. Execution Jobs (ASQE-QAMA-General pool):
#    - Runs tests in parallel using matrix strategy
#    - Routes to specific agents using Platform demand with agentName from matrix
#    - Matrix variable $(agentName) is expanded BEFORE queue time via dependency outputs
#    - Sets CURRENT_PLATFORM and CONNECTED_DEVICE environment variables
#    - Clear timeout errors for unavailable agent combinations
#
# Features:
# - AST-based marker extraction with class/method inheritance (method overrides class)
# - Strict validation: fails if tests missing required markers
# - Azure DevOps API integration for dynamic agent discovery
# - Platform×device cartesian product generation
# - Agent deduplication (first available per platform×device combo)
# - Pool demands with dependency output variables (expanded before queue time)
# - Dynamic marker creation for pytest -m filtering (platform_<name>, device_<name>)
# - Clear error messages for troubleshooting
#
# Pytest Marker Filtering:
# The conftest.py hook creates dynamic markers from platform/device arguments:
#   @pytest.mark.platform("cashmerexi", "machu13x") creates:
#     - @pytest.mark.platform_cashmerexi
#     - @pytest.mark.platform_machu13x
#   @pytest.mark.connected_device("roo") creates:
#     - @pytest.mark.device_roo
#
# Use -m expressions in pytestArgs to filter tests:
#   pytestArgs: '-v --tb=short -m "platform_machu13x and device_roo"'
#   pytestArgs: '-v --tb=short -m "platform_cashmerexi or platform_machu13x"'
#   pytestArgs: '-v --tb=short -m "device_roo and not platform_cashmerexi"'
#
# Required Variable Groups:
# - secrets: Contains AzureDevOpsPAT for Azure DevOps API authentication
# - HP App Build Pipeline Variables: Contains Project ID for build downloads
#
# Usage Example:
#   - template: templates/job templates/marker-based-test-execution-job.yml
#     parameters:
#       suitePath: 'tests/hp_app/pen_control'
#       pipelineId: '$(Production)'
#       pytestArgs: '-v --tb=short -m "platform_machu13x and device_roo"'

parameters:
- name: workingDirectory
  type: string
  default: ''
  displayName: 'Working Directory for steps (optional)'
- name: suitePath
  type: string
  displayName: 'Test Suite Path'

- name: artifactNamePrefix
  type: string
  default: ''
  displayName: 'Artifact name prefix (e.g., mat-build-)'

- name: pipelineId
  type: string
  default: '$(Production)'
  displayName: 'Pipeline ID for myHP App Installation'

- name: buildId
  type: string
  default: ''
  displayName: 'Build ID (numeric) for myHP App Installation'

- name: buildVersion
  type: string
  default: ''
  displayName: 'Build Version (string like 53.42601.121.0) for myHP App Installation'

- name: pytestArgs
  type: string
  default: '-v --tb=short'
  displayName: 'Additional pytest Arguments'

- name: milestoneType
  type: string
  default: ''
  displayName: 'TestRail Milestone Type (Daily or RC, empty to skip milestone creation)'

- name: testFilter
  type: string
  default: ''
  displayName: 'Test filter for pytest -k option'

- name: testrailRunId
  type: string
  default: ''
  displayName: 'TestRail Run ID (optional, for re-execution or RC runs)'

- name: additionalMarkerFilter
  type: string
  default: ''
  displayName: 'Additional Marker Filter (combined with automatic platform/device filter using AND)'

- name: requireRobotics
  type: boolean
  default: false
  displayName: 'Restrict agent selection to agents with Robotics=True capability'

- name: moduleName
  type: string
  default: ''
  displayName: 'Module Name (used to retrieve modified test files from stage outputs)'

- name: useOtaInstall
  type: boolean
  default: false
  displayName: 'Use OTA Installation (baseline→upgrade) instead of clean install'

- name: checkoutImageBank
  type: boolean
  default: false
  displayName: 'Checkout imagebank repository for image comparison tests'

jobs:
# ============================================================================
# Job 1: Discovery - Parse Markers and Generate Agent Matrix
# ============================================================================
# Purpose: Analyzes test files and creates execution matrix based on pytest markers
# Pool: ASQE-QAMA-VM (dedicated for discovery to avoid blocking test execution agents)
# Outputs:
# - DiscoverAgents.agentMatrix: JSON matrix for parallel execution strategy
# - DiscoverAgents.foundAgents: Boolean indicating if matrix was generated
# - DiscoverAgents.agentCount: Number of matrix entries
- job: DiscoverAgents
  displayName: "Discover Compatible Agents for ${{ replace(parameters.suitePath, '$(testSuiteBasePath)/', '') }}"
  pool:
    name: ASQE-QAMA-VM
  
  variables:
    requirementsFile: '$(Build.ArtifactStagingDirectory)/marker-requirements.json'
    ${{ if ne(parameters.moduleName, '') }}:
      specificTestFiles: $[ stageDependencies.AnalyzePR.ProcessPR.outputs['AnalyzeFiles.testfiles_${{ parameters.moduleName }}'] ]
    ${{ if eq(parameters.moduleName, '') }}:
      specificTestFiles: ''
  
  steps:
  - checkout: self
    displayName: 'Checkout Test Repository (Clean)'
    clean: true

  # Step 1a: Download TestRail categorization (if testrailRunId provided)
  # Purpose: Get case IDs for this suite from PrepareTestRun stage artifact
  # Artifact: testrail-suite-categorization.json contains suite-level categorization
  # Output: Sets testrailCaseIds pipeline variable with comma-separated case IDs for this suite
  - ${{ if ne(parameters.testrailRunId, '') }}:
    - task: DownloadPipelineArtifact@2
      displayName: 'Download TestRail Categorization'
      inputs:
        source: 'current'
        artifact: 'TestRail Categorization'
        path: '$(Pipeline.Workspace)/testrail-categorization'
  
  - ${{ if ne(parameters.testrailRunId, '') }}:
    - task: PowerShell@2
      name: ParseTestRailCases
      displayName: 'Extract Case IDs for ${{ replace(parameters.suitePath, ''$(testSuiteBasePath)/'', '''') }}'
      inputs:
        targetType: 'inline'
        script: |
          $jsonPath = "$(Pipeline.Workspace)/testrail-categorization/testrail-suite-categorization.json"
          $categorization = Get-Content $jsonPath -Raw | ConvertFrom-Json
          
          # Extract suite name from path (e.g., "tests/hp_app/pen_control" -> "pen_control")
          $suitePath = "${{ parameters.suitePath }}"
          $suiteName = $suitePath.Split('/')[-1]
          
          $caseIds = ""
          if ($categorization.suites.$suiteName) {
            $caseIds = $categorization.suites.$suiteName.case_ids -join ','
            $count = $categorization.suites.$suiteName.count
            Write-Host "##[section]Suite '$suiteName' has $count test cases from TestRail run ${{ parameters.testrailRunId }}"
            Write-Host "Case IDs: $caseIds"
          } else {
            Write-Host "##[warning]Suite '$suiteName' has no tests in TestRail run ${{ parameters.testrailRunId }}"
          }
          
          Write-Host "##vso[task.setvariable variable=testrailCaseIds;isOutput=true]$caseIds"
  
  # Step 1b: Parse test markers from Python files
  # Purpose: Extract @pytest.mark.platform() and @pytest.mark.connected_device() markers
  # Script: parse-test-markers.py - uses AST to parse markers, validates presence, generates cartesian product
  # Validation: Shows warnings if any tests missing required markers, continues with valid tests
  # Specific Files: If moduleName parameter provided, retrieves modified files from stage outputs
  # TestRail Filtering: If testrailRunId provided, filters tests to only those in the TestRail run
  - ${{ if ne(parameters.moduleName, '') }}:
    - ${{ if ne(parameters.testrailRunId, '') }}:
      - task: PythonScript@0
        displayName: 'Parse Test Markers from ${{ parameters.suitePath }} (Modified Files + TestRail Filter)'
        continueOnError: true
        inputs:
          scriptSource: 'filePath'
          scriptPath: '$(Build.Repository.LocalPath)/devops/utils/test-execution/parse-test-markers.py'
          arguments: '--suite-path "${{ parameters.suitePath }}" --output "$(requirementsFile)" --specific-files "$(specificTestFiles)" --testrail-cases "$(ParseTestRailCases.testrailCaseIds)"'
          workingDirectory: '$(Build.Repository.LocalPath)'
          failOnStderr: false
    - ${{ if eq(parameters.testrailRunId, '') }}:
      - task: PythonScript@0
        displayName: 'Parse Test Markers from ${{ parameters.suitePath }} (Modified Files Only)'
        continueOnError: true
        inputs:
          scriptSource: 'filePath'
          scriptPath: '$(Build.Repository.LocalPath)/devops/utils/test-execution/parse-test-markers.py'
          arguments: '--suite-path "${{ parameters.suitePath }}" --output "$(requirementsFile)" --specific-files "$(specificTestFiles)"'
          workingDirectory: '$(Build.Repository.LocalPath)'
          failOnStderr: false
  
  - ${{ if eq(parameters.moduleName, '') }}:
    - ${{ if ne(parameters.testrailRunId, '') }}:
      - task: PythonScript@0
        displayName: 'Parse Test Markers from ${{ parameters.suitePath }} (TestRail Filter)'
        continueOnError: true
        inputs:
          scriptSource: 'filePath'
          scriptPath: '$(Build.Repository.LocalPath)/devops/utils/test-execution/parse-test-markers.py'
          arguments: '--suite-path "${{ parameters.suitePath }}" --output "$(requirementsFile)" --testrail-cases "$(ParseTestRailCases.testrailCaseIds)"'
          workingDirectory: '$(Build.Repository.LocalPath)'
          failOnStderr: false
    - ${{ if eq(parameters.testrailRunId, '') }}:
      - task: PythonScript@0
        displayName: 'Parse Test Markers from ${{ parameters.suitePath }}'
        continueOnError: true
        inputs:
          scriptSource: 'filePath'
          scriptPath: '$(Build.Repository.LocalPath)/devops/utils/test-execution/parse-test-markers.py'
          arguments: '--suite-path "${{ parameters.suitePath }}" --output "$(requirementsFile)"'
          workingDirectory: '$(Build.Repository.LocalPath)'
          failOnStderr: false
  
  # Step 2: Create or get TestRail milestone (if milestoneType provided)
  # Purpose: Get milestone ID for test run creation
  # Script: create-or-get-testrail-milestone.ps1 - creates or retrieves milestone based on current date
  # Output: Sets TESTRAIL_MILESTONE_ID pipeline variable
  - ${{ if ne(parameters.milestoneType, '') }}:
    - task: PowerShell@2
      name: CreateMilestone
      displayName: 'Create or Get TestRail ${{ parameters.milestoneType }} Milestone'
      env:
        TESTRAIL_API_KEY: $(TESTRAIL_API_KEY)
        TESTRAIL_USER_NAME: $(TESTRAIL_USER_NAME)
        TESTRAIL_URL: $(TESTRAIL_URL)
        TESTRAIL_PROJECT_ID: $(TESTRAIL_PROJECT_ID)
      inputs:
        targetType: 'filePath'
        filePath: '$(Build.Repository.LocalPath)/devops/utils/test-execution/create-or-get-testrail-milestone.ps1'
        arguments: '-milestoneType "${{ parameters.milestoneType }}" -testrailUrl "$(TESTRAIL_URL)" -testrailUser "$(TESTRAIL_USER_NAME)" -testrailApiKey "$(TESTRAIL_API_KEY)" -projectId "$(TESTRAIL_PROJECT_ID)"'
        workingDirectory: '$(Build.Repository.LocalPath)/devops/utils/test-execution'
  
  # Step 3: Generate agent matrix from requirements
  # Purpose: Query Azure DevOps API for agent capabilities and match to test requirements
  # Script: generate-marker-matrix.ps1 - authenticates with PAT, queries ASQE-QAMA-General pool, generates matrix
  # Authentication: Uses $(AzureDevOpsPAT) from 'secrets' variable group
  # Output: Sets agentMatrix, foundAgents, and agentCount pipeline variables
  # Warnings: Shows orange status if platform×device combinations have no matching agents
  - ${{ if parameters.requireRobotics }}:
    - task: PowerShell@2
      name: DiscoverAgents
      displayName: 'Generate Agent Execution Matrix (Robotics agents only)'
      continueOnError: true
      env:
        AZUREDEVOPSPAT: $(AzureDevOpsPAT)
      inputs:
        targetType: 'filePath'
        filePath: '$(Build.SourcesDirectory)/devops/utils/test-execution/generate-marker-matrix.ps1'
        arguments: '-RequirementsFile "$(requirementsFile)" -AdditionalMarkerFilter "${{ parameters.additionalMarkerFilter }}" -RequireRobotics'
        workingDirectory: '$(Build.SourcesDirectory)/devops/utils/test-execution'
        failOnStderr: false
  - ${{ if not(parameters.requireRobotics) }}:
    - task: PowerShell@2
      name: DiscoverAgents
      displayName: 'Generate Agent Execution Matrix'
      continueOnError: true
      env:
        AZUREDEVOPSPAT: $(AzureDevOpsPAT)
      inputs:
        targetType: 'filePath'
        filePath: '$(Build.SourcesDirectory)/devops/utils/test-execution/generate-marker-matrix.ps1'
        arguments: '-RequirementsFile "$(requirementsFile)" -AdditionalMarkerFilter "${{ parameters.additionalMarkerFilter }}"'
        workingDirectory: '$(Build.SourcesDirectory)/devops/utils/test-execution'
        failOnStderr: false

# ============================================================================
# Job 2: Test Execution - Marker-Based Matrix Strategy
# ============================================================================
# Purpose: Executes tests in parallel across agents matching marker requirements
# Dependencies: Requires DiscoverAgents job to complete successfully
# Strategy: Uses matrix from DiscoverAgents.agentMatrix for parallel execution
# Pool: ASQE-QAMA-General with Platform demand from matrix variable
# Agent Selection: Platform demand uses agentName from matrix (expanded before queue time)
# Timeout: 60 minutes (shows clear error for unavailable agent combinations)
# Condition: Only runs if tests were found (foundTests=true) and agents are available (foundAgents=true)
- job: TestExecution_Markers
  displayName: 'Executing on'
  dependsOn: DiscoverAgents
  condition: and(in(dependencies.DiscoverAgents.result, 'Succeeded', 'SucceededWithIssues'), eq(dependencies.DiscoverAgents.outputs['DiscoverAgents.foundTests'], 'true'), eq(dependencies.DiscoverAgents.outputs['DiscoverAgents.foundAgents'], 'true'))
  timeoutInMinutes: 240
  cancelTimeoutInMinutes: 5
  
  strategy:
    matrix: $[ dependencies.DiscoverAgents.outputs['DiscoverAgents.agentMatrix'] ]
  
  pool:
    name: ASQE-QAMA-General
    demands:
    - Agent.Name -equals $(agentName)
  
  variables:
    # Matrix variables from DiscoverAgents job:
    # - platformName: Platform capability value (e.g., "cashmerexi")
    # - deviceName: Connected device capability (e.g., "roo")
    # - agentName: Target agent name for display
    # - agentPlatform: Agent's Platform capability (expanded before queue time for demands)
    # Additional variables:
    # - specificTestFiles: List of specific test files to execute (from stage outputs if moduleName provided)
    ${{ if ne(parameters.moduleName, '') }}:
      specificTestFiles: $[ stageDependencies.AnalyzePR.ProcessPR.outputs['AnalyzeFiles.testfiles_${{ parameters.moduleName }}'] ]
    ${{ if eq(parameters.moduleName, '') }}:
      specificTestFiles: ''
    ${{ if ne(variables.deviceName, '') }}:
      deviceSuffixForResults: '-$(deviceName)'
      deviceSuffixForRuns: ' x $(deviceName)'
    ${{ if eq(variables.deviceName, '') }}:
      deviceSuffixForResults: ''
      deviceSuffixForRuns: ''
    # - agentAvailable: Boolean indicating if agent exists
    # - displayName: Formatted display name (e.g., "Executing on Cashmerexi with Roo")
    
    # Set repository root - always use Build.SourcesDirectory since we use explicit checkout: self
    # (explicit checkout always uses Build.SourcesDirectory, not Build.Repository.LocalPath)
    repoRoot: '$(Build.SourcesDirectory)'
    
    # Environment variables for pytest runtime
    CURRENT_PLATFORM: $(platformName)
    CONNECTED_DEVICE: $(deviceName)
    
    # TestRail integration variables (conditionally set from DiscoverAgents job outputs)
    ${{ if ne(parameters.milestoneType, '') }}:
      TESTRAIL_MILESTONE_ID: $[ dependencies.DiscoverAgents.outputs['CreateMilestone.TESTRAIL_MILESTONE_ID'] ]
    ${{ if ne(parameters.testrailRunId, '') }}:
      TESTRAIL_RUN_ID: ${{ parameters.testrailRunId }}
      TESTRAIL_CASE_IDS: $[ dependencies.DiscoverAgents.outputs['ParseTestRailCases.testrailCaseIds'] ]
  
  steps:
  
  # Step 2: Checkout imagebank repository (if enabled)
  # Purpose: Check out baseline images from asqe-hp-app-imagebank repository
  # Note: Only runs when checkoutImageBank parameter is true and repository is defined in pipeline resources
  - ${{ if eq(parameters.checkoutImageBank, true) }}:
    - checkout: imagebank
      displayName: 'Checkout Image Bank Repository'
  
  # Step 4: Checkout main test repository
  - checkout: self
    displayName: 'Checkout Test Repository'
    clean: true
  
  # Step 4b: Move repository contents up one level
  # Purpose: Move contents from Build.SourcesDirectory/asqe-qama/ to Build.SourcesDirectory/
  # Reason: Multi-repository checkout places repo in subfolder; scripts expect flat structure
  - ${{ if eq(parameters.checkoutImageBank, true) }}:
    - task: PowerShell@2
      displayName: 'Flatten Repository Directory Structure'
      inputs:
        targetType: 'inline'
        script: |
          $ErrorActionPreference = "Stop"
          $repoSubDir = "$(Build.SourcesDirectory)/asqe-qama"
          $targetDir = "$(Build.SourcesDirectory)"
          
          Write-Host "Checking for nested repository structure..."
          
          if (Test-Path $repoSubDir) {
            Write-Host "Found repository at: $repoSubDir"
            Write-Host "Moving contents to: $targetDir"
            
            # Move each item from asqe-qama subfolder to parent directory
            # This preserves other folders like imagebank that may exist in Build.SourcesDirectory
            Get-ChildItem -Path $repoSubDir -Force | ForEach-Object {
              $destination = Join-Path $targetDir $_.Name
              Write-Host "Moving: $($_.Name)"
              
              # If destination exists, remove it first (handles conflicts)
              if (Test-Path $destination) {
                Remove-Item -Path $destination -Recurse -Force -ErrorAction SilentlyContinue
              }
              
              Move-Item -Path $_.FullName -Destination $targetDir -Force
            }
            
            # Remove the now-empty asqe-qama directory
            Remove-Item -Path $repoSubDir -Force -ErrorAction SilentlyContinue
            
            Write-Host "Repository structure flattened successfully"
            Write-Host "Repository root is now: $targetDir"
            
            # Verify critical files exist after flatten
            Write-Host ""
            Write-Host "Verifying directory structure after flatten:"
            if (Test-Path "$targetDir/tests/hp_app/conftest.py") {
              Write-Host "[OK] tests/hp_app/conftest.py exists"
            } else {
              Write-Host "[ERROR] tests/hp_app/conftest.py is MISSING!"
            }
            if (Test-Path "$targetDir/tests/conftest.py") {
              Write-Host "[OK] tests/conftest.py exists"
            } else {
              Write-Host "[ERROR] tests/conftest.py is MISSING!"
            }
            if (Test-Path "$targetDir/tests/pytest.ini") {
              Write-Host "[OK] tests/pytest.ini exists"
            } else {
              Write-Host "[ERROR] tests/pytest.ini is MISSING!"
            }
          } else {
            Write-Host "Repository already at correct location: $targetDir"
          }
      continueOnError: false
  
  # Step 5: Update job display name with formatted text
  # Purpose: Show readable execution context in job name
  # Script: update-job-display-name.ps1
  - task: PowerShell@2
    displayName: 'Update Job Display Name'
    inputs:
      targetType: 'filePath'
      filePath: '$(repoRoot)/devops/utils/test-execution/update-job-display-name.ps1'
      arguments: '-DisplayName "$(displayName)"'
  
  # Step 3: Display execution context
  # Purpose: Show which platform×device combination is being executed
  # Script: display-execution-context.ps1
  - task: PowerShell@2
    displayName: 'Display Execution Context'
    inputs:
      targetType: 'filePath'
      filePath: '$(repoRoot)/devops/utils/test-execution/display-execution-context.ps1'
      arguments: '-PlatformName "$(platformName)" -DeviceName "$(deviceName)" -AgentPlatform "$(agentPlatform)" -AgentName "$(agentName)" -ActualAgentName "$(Agent.Name)" -ActualMachineName "$(Agent.MachineName)"'
  
  # Step 4: Check and start Appium server
  # Purpose: Ensures WinAppDriver Appium server is running for UI automation
  # Script: $(appiumScriptPath) - checks if Appium running; starts if not
  - task: PowerShell@2
    displayName: 'Check and Start Appium Server'
    inputs:
      targetType: 'filePath'
      filePath: $(appiumScriptPath)
      arguments: '-AgentName "$(Agent.Name)" -Mode "marker-based"'
      workingDirectory: '$(repoRoot)/devops/utils'
  
  # Step 5: Install myHP application
  # Purpose: Downloads and installs myHP app package from Azure DevOps builds
  # Template: install-myhp.yml - handles version checking, download, certificate install, package deployment
  - template: ../step templates/install-myhp.yml
    parameters:
      pipelineId: ${{ parameters.pipelineId }}
      buildVersion: ${{ parameters.buildVersion }}
      buildId: ${{ parameters.buildId }}
      ${{ if or(ne(parameters.buildId, ''), ne(parameters.buildVersion, '')) }}:
        buildVersionToDownload: specific
      ${{ else }}:
        buildVersionToDownload: latest
  
  # Step 6: Set test execution context
  # Purpose: Display context information in logs for debugging
  # Script: display-test-context.ps1
  - task: PowerShell@2
    displayName: 'Set Test Execution Context'
    inputs:
      targetType: 'filePath'
      filePath: '$(repoRoot)/devops/utils/test-execution/display-test-context.ps1'
      arguments: '-SuitePath "${{ parameters.suitePath }}" -PlatformMarker "$env:CURRENT_PLATFORM" -DeviceMarker "$env:CONNECTED_DEVICE" -WorkingDirectory "$(repoRoot)"'
  
  # Step 7: Collect tests (show which tests will run)
  # Purpose: Display which tests will be executed before running them
  # Uses pytest --collect-only to list tests matching the marker filter
  # Script: collect-tests.ps1
  - ${{ if ne(variables.TESTRAIL_CASE_IDS, '') }}:
      - task: PowerShell@2
        displayName: 'Collect Tests for $(platformName) × $(deviceName)'
        continueOnError: true
        env:
          CURRENT_PLATFORM: $(platformName)
          CONNECTED_DEVICE: $(deviceName)
          PYTHONPATH: $(System.DefaultWorkingDirectory)
        inputs:
          targetType: 'filePath'
          filePath: '$(Build.Repository.LocalPath)/devops/utils/test-execution/collect-tests.ps1'
          arguments: '-SuitePath "${{ parameters.suitePath }}" -PlatformName "$(platformName)" -DeviceName "$(deviceName)" -PytestArgs "${{ parameters.pytestArgs }}" -AdditionalMarkerFilter "${{ parameters.additionalMarkerFilter }}" -WorkingDirectory "${{ parameters.workingDirectory }}" -SpecificTestFiles "$(specificTestFiles)" -TestrailCaseIds "$(TESTRAIL_CASE_IDS)"'
          workingDirectory: '${{ parameters.workingDirectory }}'
  - ${{ if eq(variables.TESTRAIL_CASE_IDS, '') }}:
      - task: PowerShell@2
        displayName: 'Collect Tests for $(platformName) $(deviceSuffixForRuns)'
        continueOnError: true
        env:
          CURRENT_PLATFORM: $(platformName)
          CONNECTED_DEVICE: $(deviceName)
          PYTHONPATH: $(System.DefaultWorkingDirectory)
        inputs:
          targetType: 'filePath'
          filePath: '$(Build.Repository.LocalPath)/devops/utils/test-execution/collect-tests.ps1'
          arguments: '-SuitePath "${{ parameters.suitePath }}" -PlatformName "$(platformName)" -DeviceName "$(deviceName)" -PytestArgs "${{ parameters.pytestArgs }}" -AdditionalMarkerFilter "${{ parameters.additionalMarkerFilter }}" -WorkingDirectory "${{ parameters.workingDirectory }}" -SpecificTestFiles "$(specificTestFiles)"'
          workingDirectory: '${{ parameters.workingDirectory }}'
  
  # Step 8: Execute pytest tests
  # Purpose: Runs pytest test suite with marker-based filtering
  # Template: run-pytest-suite.yml - handles pytest execution with environment setup
  # Environment: CURRENT_PLATFORM and CONNECTED_DEVICE available to pytest hooks
  # Marker Filter: Automatically adds -m "platform_<platformName>" or "platform_<platformName> and device_<deviceName>"
  # Additional Filter: If additionalMarkerFilter is provided, combines with: "(platform_X [and device_Y]) and (additionalFilter)"
  # Script: build-marker-filter.ps1
  - task: PowerShell@2
    displayName: 'Build Marker Filter'
    name: BuildMarkerFilter
    inputs:
      targetType: 'filePath'
      filePath: '$(repoRoot)/devops/utils/test-execution/build-marker-filter.ps1'
      arguments: '-PlatformName "$(platformName)" -DeviceName "$(deviceName)" -AdditionalMarkerFilter "${{ parameters.additionalMarkerFilter }}"'
  
  # Step 7: Execute pytest tests
  - powershell: |
      $date = Get-Date -Format "yyyyMMdd-HHmmss"
      Write-Host "##vso[task.setvariable variable=dateSuffix]$date"
    displayName: 'Set Date Suffix Variable'
    condition: always()
  - template: ../step templates/run-pytest-suite.yml
    parameters:
      suitePath: ${{ parameters.suitePath }}
      testPattern: 'test_*.py'
      pytestArgs: '${{ parameters.pytestArgs }} --junit-xml=$(Agent.TempDirectory)/test-results-$(platformName)$(deviceSuffixForResults)-$(dateSuffix).xml'
      markerFilter: '$(BuildMarkerFilter.markerFilter)'
      testTarget: 'localhost'
      workingDirectory: '$(repoRoot)'
      specificTestFiles: '$(specificTestFiles)'
      testFilter: ${{ parameters.testFilter }}
      ${{ if ne(parameters.testrailRunId, '') }}:
        testrailCaseIds: '$(TESTRAIL_CASE_IDS)'
        testrailRunId: ${{ parameters.testrailRunId }}
      ${{ if ne(parameters.milestoneType, '') }}:
        testrailMilestoneId: '$(TESTRAIL_MILESTONE_ID)'
  
  # Step 8a: Publish test results XML as pipeline artifact FIRST
  # Purpose: Make test result XML files available for summary stage aggregation
  # CRITICAL: Must happen BEFORE PublishTestResults@2 which may modify the XML
  - task: PublishPipelineArtifact@1
    displayName: 'Publish Test Results XML'
    condition: always()
    continueOnError: true

    inputs:
      targetPath: '$(Agent.TempDirectory)/test-results-$(platformName)$(deviceSuffixForResults)-$(dateSuffix).xml'
      artifactName: '${{ parameters.artifactNamePrefix }}test-results-$(platformName)$(deviceSuffixForResults)-$(dateSuffix)'
      publishLocation: 'pipeline'
  
  # Step 8b: Publish test results to Tests tab
  # Purpose: Make test results visible in Azure DevOps Tests tab
  # Note: This task may modify the XML, so artifact publish must happen first
  - task: PublishTestResults@2
    displayName: 'Publish Test Results to Tests Tab'
    condition: always()
    inputs:
      testResultsFormat: 'JUnit'
      testResultsFiles: '$(Agent.TempDirectory)/test-results-$(platformName)$(deviceSuffixForResults)-$(dateSuffix).xml'
      testRunTitle: 'Tests: $(platformName)$(deviceSuffixForRuns)'
      mergeTestResults: false
      failTaskOnFailedTests: false
  
  # Step 9: Publish test artifacts (screenshots, recordings, logs)
  # Purpose: Make test artifacts available for download and review
  - task: PublishBuildArtifacts@1
    displayName: 'Publish Test Artifacts'
    condition: always()
    continueOnError: true
    inputs:
      PathtoPublish: '$(repoRoot)/results'
      ArtifactName: 'test-artifacts-$(platformName)$(deviceSuffixForResults)-$(dateSuffix)'
      publishLocation: 'Container'
